(function(angular) {
	'use strict';

	angular
		.module('conowGrid')
		.factory('conowGridClass', ['$http', 
			function($http) {
				function conowGridClass(){
					// 
				};

				return conowGridClass;
			}
		]);
}(angular));